name: internationalization1
description: A new Flutter application.



publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # Add this line
    sdk: flutter



  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.5 # 状態保存。再起動した時に言語の状態が維持される


dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:


  uses-material-design: true
  assets:
    - lib/lang/en.json
    - lib/lang/ja.json

